extends layout

block content
  h3 Welcome to roots!
  :markdown
    Find tutorials and documentation at http://roots.cx : )

  // Once you've added a content type and posts into Contentful
  // go update your app.coffee config and then test out iterating
  // over your content type here!

  ul
    - for post in contentful.posts
      li
        img(src!= asset(post.image, {w: 100, h: 100, q: 50}))
        h4= post.title
        p= asset(post.image)
        p= post.date
        != marked(post.body)
